extends ../layout
block content
    div.container
        div.row
            div.col-md-6
                h1 Todos
                input(
                    type="text"
                    class="form-control"
                    placeholder="Add Todo"
                )
                button.btn.btn-primary Mark all as done